{
  "name": "fisco-site",
  "version": "0.1.0",
  "private": true,
  "description": "Static site for FISCO — fiscal compliance observatory for IFIs.",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "clean": "rimraf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "rimraf": "^5.0.5"
  }
}
